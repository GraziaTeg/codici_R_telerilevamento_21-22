#03.03.22

#HO FATTO SU R STUDIO POI COPIO TUTTO SU R

#Come funziona un linguaggio di programmazione:
#- FUNZIONI = installa il pacchetto 
#- ARGOMENTI = quale pacchetto vogliamo installare

#La funzione per installare il pacchetto?
#da Google in inglese install packages R software
#install.packages("...")
#. ==> dots
#FUNZIONE = install.packages 
#ARGOMENTO = ("...") = pacchetto che dobbiamo installare
#PACCHETTO = un pezzo di Software che mi permette di utilizzare 
#funzioni in più rispetto a quello che già uso 

#su jambord(1)

#il nostro pacchetto si chiama raster tra virgolette "..."
install.packages("raster")
#in R quando ho dato l'invio per istallare il pacchetto 
#si Ã¨ aperta una finestra ed ho scelto = Italia Milano 
#library() Ã¨ una funzione di base oppure require()
library(raster)#senza virgolette perchè è già dentro a R 
#Caricamento del pacchetto richiesto: sp
#pacchetto sp = gestione dei dati spaziali 
